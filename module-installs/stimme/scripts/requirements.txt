# Stimme: Kern-Abhaengigkeiten (Pflicht)
aiogram>=3.4
claude-agent-sdk>=0.1
python-dotenv>=1.0
markdown>=3.5
httpx>=0.27

# Optional, je nach Scoping separat installieren:
# pip install weasyprint    # PDF-Report-Erzeugung
# pip install matplotlib    # Diagramme und Daten-Visualisierung
# Sprachnachrichten brauchen kein extra Paket - laufen ueber OpenRouter via httpx.
