# Friday Agentic OS — Kern-Abhängigkeiten

# Claude Code Agent SDK (benötigt einmalig: claude login)
claude-agent-sdk>=0.1

# Autonomer Scheduler
apscheduler>=3.10.0

# Umgebungsvariablen
python-dotenv>=1.0.0

# Telegram Bot (bereits im Stimme-Modul, hier für direkten Scheduler-Zugriff)
aiogram>=3.4

# HTTP (für zukünftige API-Integrationen)
httpx>=0.27
